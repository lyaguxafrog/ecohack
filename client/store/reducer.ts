import { combineReducers } from '@reduxjs/toolkit';

// import { authorization } from '@/pages/authorization/model/slice';

export const rootReducer = combineReducers({
  // [ReducerNamespace['sideWindow']]: sideWindow.reducer,
});
